Java based website and create github workflow::
1.Create Java Project
Make a simple Maven project with pom.xml and Hello.java
Hello.java prints “Build Successful”
2.push code to git:
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <repo-url>
git push -u origin main
3.Add GitHub Actions
Create folder: .github/workflows(Inside your project, create a folder:.github/workflows,Inside that folder, create a file named:maven-build.yml)

Create file: maven-build.yml
4.Configure Workflow:
name: Java Maven Build
on: push:
      branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-java@v4
      with:
        java-version: '8'
        distribution: 'temurin'
    - run: mvn clean package

5.Push Workflow:
git add .github/workflows/maven-build.yml
git commit -m "Add workflow"
git push

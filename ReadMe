docker in ec2 :
Launch Ubuntu EC2 instance (allow ports 22 & 80)
ssh -i keypair.pem ubuntu@<EC2-Public-IP> or git clone git@github.com:sakshichikorde315-sys/sakshi.git

sudo apt update -y
sudo apt install docker.io -y
sudo systemctl start docker
sudo systemctl enable docker
docker --version
sudo usermod -aG docker ubuntu
Logout and login again
docker pull nginx
docker images
docker run -d -p 80:80 --name webserver nginx
docker ps
Open browser → http://<EC2-Public-IP>


only docker commands:
serach hello world open terminal
docker pull hello-world
docker images
docker run hello-world //prints hello world docker 

to delete:
docker rmi hello-world //this will generate a numper copy that
docker rm that number
docker rmi hello-world
